parameters:
  kapitan:
    compile:
      - input_paths:
          - vault/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - vault/component/main.jsonnet
        input_type: jsonnet
        output_path: vault/

